DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

SECRET_KEY=replace_with_a_long_random_secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

FRONTEND_ORIGINS=http://localhost:5173,https://frontend-beryl-rho-37.vercel.app
COOKIE_SECURE=false
COOKIE_SAMESITE=strict

AWS_ACCESS_KEY_ID=replace_with_new_aws_key
AWS_SECRET_ACCESS_KEY=replace_with_new_aws_secret
AWS_REGION=ap-south-1
FACE_MATCH_THRESHOLD=60

FACE_DATA_DIR=face_data

# SMTP Configuration (Optional: fallback to console logging if empty)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# Frontend Application URL (for generating password reset links)
FRONTEND_URL=http://localhost:5173
